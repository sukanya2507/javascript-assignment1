
const base = prompt(' base of the triangle: ');
const height = prompt('height of the triangle: ');
const areaValue = (base * height) / 2;
console.log(
  `The area of the triangle is ${areaValue}`
);